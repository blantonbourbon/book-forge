#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/_env.sh"

unformatted=$(gofmt -l .)
if [ -n "$unformatted" ]; then
  echo "Unformatted Go files:"
  echo "$unformatted"
  exit 1
fi
npm run format:check --prefix frontend --if-present
